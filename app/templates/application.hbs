<h2 id="title">Welcome to Ember.js - With Authentication!</h2>

{{#if session.isAuthenticated}}
  Hi, {{session.currentUser.email}} <button {{action 'invalidateSession'}}>Logout</button>
{{else}}
  {{#link-to "login"}}Login{{/link-to}}
{{/if}}

{{outlet}}
{{outlet 'modal'}}