{{#each stars as |star|}}
  <span class="star-rating glyphicon {{if star.full 'glyphicon-star' 'glyphicon-star-empty'}}" {{action "set" star.rating}}>
    <div class="description-wrapper">
      <span class="description">{{star.description}}</span>
    </div>
  </span>
{{/each}}